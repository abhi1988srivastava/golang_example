//command line arguments
//first argument is the path of the file.and then the arguments

package main

import (
    "fmt"
    "os"
)


func main() {
    all_arg_no_path:=os.Args[1:]
    all_arg:=os.Args

    first_arg:=os.Args[1]
}

